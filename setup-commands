# Make sure Ruby is installed and up to date
# If you're on macOS, you might need to install a newer Ruby version
# via Homebrew or rbenv as the system Ruby can be problematic

# Install bundler if not already installed
gem install bundler

# Create a new Jekyll site
jekyll new lentrepreneur
cd lentrepreneur

# Add Tailwind CSS
npm init -y
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init 